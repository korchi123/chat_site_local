import { observer } from 'mobx-react-lite';

const ProfileBirthDate = observer(({ store }) => {
    return (
        
            
                <div>
                    string
                </div>
            
        
    );
});

export default ProfileBirthDate;